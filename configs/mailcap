image/*; open %s;
application/pdf; open %s;

text/html; pandoc --columns 120 -f html -t markdown ; copiousoutput ; compose=vim %s; description=HTML Text; test=type pandoc >/dev/null

application/octet-stream; open %s
